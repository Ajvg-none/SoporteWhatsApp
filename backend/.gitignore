node_modules/
.env

# Dependencias de Node
node_modules/
backend/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Variables de entorno y credenciales sensibles (¡Nunca subir al repositorio!)
.env

# Archivos generados por Prisma
# NOTA: /prisma/migrations/ ya NO se ignora — la migración
# 20260828000000_unique_open_ticket_per_cliente (índice único parcial) se versiona.

